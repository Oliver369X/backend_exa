/**
 * Módulo de Socket.IO para colaboración en tiempo real
 * 
 * Este módulo exporta las funcionalidades para la colaboración en tiempo real
 * entre usuarios que trabajan en el mismo proyecto.
 */

export { setupCollabSocket } from './collab-socket';
